"""Kindle HID Passthrough — userspace Bluetooth HID host with UHID passthrough.

Modules are loaded directly via sys.path injection by main.py / daemon.py,
not as a package. This file exists only to mark the directory.
"""
